name: build
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: run
        run: |
          ls ${{github.workspace}}/build.sh
          source ${{github.workspace}}/build.sh
